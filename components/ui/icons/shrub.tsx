import { ShrubIcon } from 'lucide-react-native';
import { IconProps } from '~/shared/types/icon-props.types';

export default function Shrub({ ...rest }: IconProps) {
  return <ShrubIcon {...rest} />;
}
