import { TrendingUpIcon } from 'lucide-react-native';
import { IconProps } from '~/shared/types/icon-props.types';

export default function TrendingUp({ ...rest }: IconProps) {
  return <TrendingUpIcon {...rest} />;
}
