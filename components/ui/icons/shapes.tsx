import { ShapesIcon } from 'lucide-react-native';
import { IconProps } from '~/shared/types/icon-props.types';

export default function Shapes({ ...rest }: IconProps) {
  return <ShapesIcon {...rest} />;
}
